import { shallow } from 'enzyme';
import { AudioContext } from 'src/';

describe('<AudioContext />', function() {

  it('renders a div wrapping its children', function() {
  });

  it('creates a new AudioContext and passes it to child context as context.audioContext', function() {
  });

});
